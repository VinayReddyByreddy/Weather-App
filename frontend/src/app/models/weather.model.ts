export interface Weather {
    temperature: number; // Current temperature
    description: string; // Weather description (e.g., cloudy, sunny)
    // Add more weather properties as needed (e.g., humidity, wind speed)
  }
  